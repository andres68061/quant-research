# Quick Start

```bash
# Terminal 1: API
conda activate quant
make api

# Terminal 2: Frontend
conda activate quant
make frontend
```

Open `http://localhost:5173`.

API docs at `http://localhost:8000/docs`.
